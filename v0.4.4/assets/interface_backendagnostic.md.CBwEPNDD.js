import{_ as a,c as t,o as s,aA as i}from"./chunks/framework.C9sUTB0a.js";const u=JSON.parse('{"title":"Backend-agnostic solver","description":"","frontmatter":{},"headers":[],"relativePath":"interface/backendagnostic.md","filePath":"interface/backendagnostic.md","lastUpdated":null}'),n={name:"interface/backendagnostic.md"};function o(l,e,c,r,d,p){return s(),t("div",null,e[0]||(e[0]=[i(`<h1 id="Backend-agnostic-solver" tabindex="-1">Backend-agnostic solver <a class="header-anchor" href="#Backend-agnostic-solver" aria-label="Permalink to &quot;Backend-agnostic solver {#Backend-agnostic-solver}&quot;">​</a></h1><p>MaterialPointSolver.jl is a high-performance solver that is backend-agnostic, meaning we only need to write a unified codebase that can run on different accelerator backends. The main computational process of the solver relies on kernel functions implemented by <a href="https://github.com/JuliaGPU/KernelAbstractions.jl" target="_blank" rel="noreferrer">KernelAbstractions.jl</a>, so users do not need to worry about the data migration process from the CPU to other hardware backends.</p><p>Users should use MaterialPointSolver.jl, and then selects the backend based on your hardware, for example:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MaterialPointSolver</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CUDA</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># using AMDGPU # for AMD   gpu</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># using Metal  # for Apple gpu</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># using oneAPI # for Intel gpu</span></span></code></pre></div><p>Taking the CUDA backend as an example, we define automatic conversion methods in <code>/ext/CUDAExt/devicehelpfunc_cuda.jl</code>: <code>host2device</code> for transferring data to the device, and <code>device2host!</code> for downloading data back to the CPU from the backend.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>The reason this solution can succeed is that the structs&#39; (including custom structs) fields only contain <code>scalar</code> and <code>array</code> types for the kernel function, and currently do not support other forms.</p></div><p>Although Julia has a garbage collection mechanism to automatically reclaim memory, we hope to clean up GPU memory usage promptly after each simulation. Currently, in <code>/ext/CUDAExt/devicehelpfunc_cuda.jl</code>, we use the function <code>clean_device!</code> to do this.</p><div class="tip custom-block"><p class="custom-block-title">Note</p><p>For all functions related to the accelerated backend that are not part of the MPM process, a dedicated implementation may be required in the <code>/ext</code> folder.</p></div><div class="tip custom-block"><p class="custom-block-title">Todo</p><p>Currently,</p><ol><li><p>Memory clean can only be achieved on the Nvidia platform, and other platforms are waiting for it to provide the relevant API.</p></li><li><p>Metal on the Apple platform does not support FP64 computation precision.</p></li><li><p>Intel&#39;s GPU devices have not been tested, but the code implementation should be relatively easy. Ideally, it should only require filling in some parts of the <code>/ext</code> folder.</p></li></ol></div>`,9)]))}const k=a(n,[["render",o]]);export{u as __pageData,k as default};
